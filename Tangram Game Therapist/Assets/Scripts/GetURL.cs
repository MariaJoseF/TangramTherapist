using UnityEngine;
using System.Collections;

public class GetURL : MonoBehaviour {

	/*void Start () {
		string url = "http://172.20.10.2:5000/";
		string urlMac = "http://192.168.1.65:5000/";
		WWW www = new WWW(urlMac);
		//WWW www = new WWW(url);
		StartCoroutine(WaitForRequest(www));
	}
	IEnumerator WaitForRequest(WWW www)
	{
		yield return www;
		// check for errors
		if (www.error == null)
		{
			Debug.Log("WWW Ok!: " + www.text);
		} else {
			Debug.Log("WWW Error: "+ www.error);
		}    
	}*/
}
